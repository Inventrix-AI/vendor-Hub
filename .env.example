# ========================================
# VENDOR HUB - ENVIRONMENT CONFIGURATION
# ========================================
# Copy this file to .env and update with your actual values

# ========================================
# DATABASE CONFIGURATION (REQUIRED)
# ========================================
# Supabase PostgreSQL Connection String
DATABASE_URL=postgresql://postgres:your-password@db.xxx.supabase.co:5432/postgres
POSTGRES_DB=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-password

# ========================================
# APPLICATION SECURITY (REQUIRED)
# ========================================
# JWT Secret Key - MUST be at least 32 characters for production
# Generate using: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-minimum-32-characters-long

# Application Secret Key
SECRET_KEY=your-secret-key-change-this-in-production-minimum-32-characters-long

# ========================================
# PAYMENT GATEWAY - RAZORPAY (REQUIRED)
# ========================================
# Get these from: https://dashboard.razorpay.com/
# Test mode keys start with: rzp_test_
# Live mode keys start with: rzp_live_
RAZORPAY_KEY_ID=rzp_test_your_key_id
RAZORPAY_KEY_SECRET=your_razorpay_secret_key

# Frontend Razorpay Key (must match RAZORPAY_KEY_ID)
NEXT_PUBLIC_RAZORPAY_KEY_ID=rzp_test_your_key_id

# ========================================
# FILE STORAGE - SUPABASE (REQUIRED)
# ========================================
# Get these from: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# ========================================
# APPLICATION URLS (REQUIRED FOR PRODUCTION)
# ========================================
# Base URL for API endpoints
API_BASE_URL=http://localhost:3000
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# Application URL (used for notifications, redirects, etc.)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ========================================
# EMAIL CONFIGURATION (OPTIONAL)
# ========================================
# SMTP Server Configuration (for sending emails)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-email-app-password
SMTP_FROM_EMAIL=noreply@yourdomain.com
SMTP_FROM_NAME=Vendor Hub

# ========================================
# SMS CONFIGURATION (OPTIONAL)
# ========================================
# Twilio Configuration (for sending SMS notifications)
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_PHONE_NUMBER=+1234567890

# ========================================
# CRON JOB CONFIGURATION (OPTIONAL)
# ========================================
# Secret key for cron job authentication
CRON_SECRET=your-cron-secret-key

# ========================================
# AWS S3 CONFIGURATION (DEPRECATED - Using Supabase Storage)
# ========================================
# These are kept for backward compatibility but not actively used
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_REGION=us-east-1
# S3_BUCKET=your-s3-bucket-name

# ========================================
# LOCAL FILE STORAGE (DEPRECATED)
# ========================================
# Local upload directory - no longer used, now using Supabase Storage
# UPLOAD_DIR=./uploads

# ========================================
# DEPLOYMENT CONFIGURATION
# ========================================
# Environment: development | production
NODE_ENV=development

# Vercel-specific (automatically set by Vercel)
# VERCEL=1
# VERCEL_URL=your-app.vercel.app
# VERCEL_ENV=production

# ========================================
# NOTES FOR PRODUCTION DEPLOYMENT
# ========================================
# 1. REQUIRED VARIABLES (Must be set):
#    - DATABASE_URL
#    - JWT_SECRET (32+ characters)
#    - RAZORPAY_KEY_ID
#    - RAZORPAY_KEY_SECRET
#    - NEXT_PUBLIC_RAZORPAY_KEY_ID
#    - NEXT_PUBLIC_SUPABASE_URL
#    - NEXT_PUBLIC_SUPABASE_ANON_KEY
#
# 2. RECOMMENDED FOR PRODUCTION:
#    - NEXT_PUBLIC_APP_URL (set to your domain)
#    - SMTP settings (for email notifications)
#    - CRON_SECRET (for automated tasks)
#
# 3. RAZORPAY MODE:
#    - For testing: use rzp_test_ keys
#    - For production: use rzp_live_ keys
#    - Never commit real API keys to version control
#
# 4. SUPABASE SETUP:
#    - Create a bucket named 'vendor-documents' in Supabase Storage
#    - Set bucket to private (not public)
#    - Enable RLS (Row Level Security) policies as needed
#
# 5. DATABASE SETUP:
#    - Run database migrations before first deployment
#    - Ensure database schema is initialized
#    - Connection pool size is optimized for serverless (max: 5)
#
# 6. VERCEL DEPLOYMENT:
#    - Set all REQUIRED variables in Vercel dashboard
#    - Max file upload size: 4.5MB (Hobby/Pro tier)
#    - Consider Vercel Enterprise for larger files
#    - Function timeout: 10s (Hobby), 60s (Pro), 900s (Enterprise)
